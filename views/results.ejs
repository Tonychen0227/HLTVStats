<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Match Results</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
  </head>
  <body>
    <fieldset>
        <h1 align="center"> Match Results </h1>
        <div class="container">
            <form action="/" method="get">
                <input type="submit" class="ghost-button" value="Back to Home">
            </form>
        </div>
    <h3> MATCH RESULTS </h3>
    <% if(matches !== null){ %>
        <ul>
            <% for(var i=0; i < matches.length; i++) { %>
                <% if(matches[i].team1 && matches[i].team2 && matches[i].event && !matches[i].live){ %>
                    <li>
                    <p><%= matches[i].team1.name %> (<%= matches[i].result.team1 %>) vs <%= matches[i].team2.name %> (<%= matches[i].result.team2 %>) on <%= matches[i].map %> @ <%= matches[i].event.name %> </p>
                    <p> <a href="results/detailedstats?id=<%= matches[i].id %>"> Detailed Stats </a></p>
                    </li>   
                <% } else {%>
                <% } %>
            <% } %>
        </ul>
    <% } %>
    <% if(error !== null){ %>
        <p><%= error %></p>
    <% } %>
    </fieldset> 
  </body>
</html>