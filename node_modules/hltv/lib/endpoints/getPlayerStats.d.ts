import FullPlayerStats from '../models/FullPlayerStats';
import HLTVConfig from '../models/HLTVConfig';
declare const getPlayerStats: (config: HLTVConfig) => ({ id, startDate, endDate }: {
    id: number;
    startDate: string;
    endDate: string;
}) => Promise<FullPlayerStats>;
export default getPlayerStats;
